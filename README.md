# zap-delivery

## Running the project

- Install the dependencies
  ```bash
    pip install -r app/requirements.txt
    ```

- Start the Docker
  ```bash
    docker-compose -f dockercompose-local.yml
    ```
  
    